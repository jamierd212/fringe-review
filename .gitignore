.venv/
__pycache__/
*.pyc
.DS_Store

# Stray clones of the GitHub repo made during setup — not part of the project
fring-review/
fringe-review/

# Kept locally as a working reference, deliberately not published.
README.md
data/outbox.json
data/instagram/

# SQLite rollback journals. Transient, machine-local, and never part of the
# committed database — one was left behind by an interrupted run and offered
# itself up for commit.
data/*.db-journal
data/*.db-wal
data/*.db-shm
